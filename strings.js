// TODO Strings
const message = 'Hello World';
// Mostrar quantos caracteres tem a variável message
console.log(message.length);
// Transformar a valor da variável em maiúsculo
console.log(message.toUpperCase());
// Transformar a valor da variável em minúsculos
console.log(message.toUpperCase());

// Cortar String em uma lista
const names = 'Walt,Johnsley';
console.log(names.split(','));

// Concatenar variáveis
const firstName = 'Walt';
const lastName = 'Johnsley';
console.log('Meu nome é ' + firstName + ' ' + lastName);
console.log(`Meu nome é ${firstName} ${lastName}`);

// TODO numbers
const number = 5;
console.log(number);

// TODO para checar o tipo de uma variável usamos o => typeof
console.log(typeof number);

// TODO Boolean
console.log(1 == 1);
console.log(1 != 1);

// TODO Null & undefined
const x = null;
const y = undefined;

// Listas
[1, 2, 3, 4, 5, 6, 7];
