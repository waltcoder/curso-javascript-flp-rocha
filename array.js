// TODO criar um Lista
const names = ['Walt', 'Johnsley', 16];
// TODO imprimindo listas no console
const firstName = names[0];
console.log(firstName);

// Adicionar items no final do Array
names.push('Pedro');
console.log(names);
// Adicionar items no início do Array
names.unshift(2005);
console.log(names);
// Remover items no final do Array
names.pop();
